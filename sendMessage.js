/* eslint-disable */
// Declarando a função sendMessage()
function sendMessage() {
    // Variavel local
    var Message = "this is an Message";
    console.log(`${Message}`);
}
exports.sendMessage = sendMessage;
