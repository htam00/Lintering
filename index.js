// Declarando a função sendMessage()
function sendMessage(){
    // Variavel
    const Message = "Hello Like Logs" 

    // loops 
    for(let i = 0; i < 5; i++){
        console.log(Message) // mostrar 5 vezes a mensagem
    }
}

// Chamando a Função sendMessage("saidaDeMessage(PT_BR)")
sendMessage()
